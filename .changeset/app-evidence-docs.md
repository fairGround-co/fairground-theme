---
"@fairground-co/fairground-theme": patch
---

Docs only, no value changes: org voice doc (docs/voice.md, drafted from
gainGround brand/product/design specs), first-consumer evidence per flagged
refinement item (gainGround React rewrite ships on this theme as of
2026-07-11), and README consumer status. The `docs/` dir ships in the
published package, hence the patch.
