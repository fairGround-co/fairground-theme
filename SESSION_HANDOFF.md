# Session handoff — first consumer live; evidence + voice doc in PR

2026-07-11 (branch `cc/2-app-evidence`, PR pending Kyle): the gainGround app
was rebuilt on core + this theme (gainground#2 / PR #3) and is the theme's
**first production consumer**. This session added, docs-only (no value
changes): `docs/voice.md` (org voice drafted from gainGround
BRAND-BRIEF/PRODUCT/DESIGN), a per-flag first-consumer evidence section in
`docs/refinement-notes.md`, and README consumer status. Changeset: patch
(docs ship in the package).

**Still open — issue #2 value refinement:** work it one value at a time WITH
Kyle against `docs/lookbook.html` (his standing instruction; do not
batch-fix). The evidence section tells the next session which flags now have
production data: `--commit` is in active use, `--surface-2` is visible in
every form, `--cat-*` staying neutral is confirmed, scope colors remain
unused. Noted for core, not this repo: apps lack a danger Button variant
(gainGround composed an app-side DangerButton — graduation candidate if a
second app repeats it).

Seed history (issue #1, 2026-07-10): values extracted from gainGround's
design spec onto contract v0, published 0.1.0. Values-only invariant
unchanged.
